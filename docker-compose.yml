services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: abstract0-backend
    ports:
      - "8000:8000"
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
    volumes:
      - ./backend:/app
      - /app/__pycache__
    networks:
      - abstract0-network
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: abstract0-frontend
    ports:
      - "8080:8080"
    environment:
      - VITE_API_BASE_URL=http://localhost:8000
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - backend
    networks:
      - abstract0-network
    restart: unless-stopped

networks:
  abstract0-network:
    driver: bridge
